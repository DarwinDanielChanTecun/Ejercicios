const celsius = parseFloat(prompt("8"));
{
    {const fahrenheit = (celsius * 1.8) + 32;
    
        console.log(celsius + " grados Celsius son equivalentes a " + fahrenheit + " grados Fahrenheit.");
        // Mostrar el resultado
console.log(`${celsius} grados Celsius son equivalentes a ${fahrenheit.toFixed(1)} grados Fahrenheit.`);
    }
}


